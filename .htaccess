Options -Indexes
RewriteEngine On

# --- Keep existing files and folders working ---
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# --- Route everything else to index.php ---
RewriteRule ^ index.php [L]